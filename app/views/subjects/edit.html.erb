<h1>Editing Subject #<%= @subject.id %></h1>


<%= form_tag(subject_url(@subject), method: 'patch') do %>

  <div>
    <%= label_tag :name %><br />
    <%= text_field_tag :name, @subject.name %>
  </div>

  
  <div>
    <%= submit_tag "Update Subject" %>
  </div>

<% end %>
